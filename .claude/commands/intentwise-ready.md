# [DEPRECATED — Intentwise MCP integration removed]
# Direct Intentwise MCP calls have been removed from this project.
# Intentwise data is now read from the Emplicit Postgres database after Intentwise syncs it there.
# This command is preserved per project no-delete policy. Do not run it.
#
# For data connectivity, confirm Postgres schema and table names with the data team.
# See tools/postgres.py — get_account_health_metrics() for the current data layer.
